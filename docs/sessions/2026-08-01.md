# Session — 2026-08-01

## Objective
- Become the shared package for the forkable Flutter template family: atomic widgets, network client, and theme infrastructure extracted from khelam (child) and forkable (base), released versioned for git-dep consumption.

## Work Log
- **Scaffold (Phase 0)**: `flutter create --template=package` at `~/projects/commons`, commit `fde167d chore: scaffold commons package`. Remote `github.com/rubenkaksh/commons` (**public**, created by user in GitHub UI); `main` pushed.
- **Manifest (Phase 1)**: scan of khelam's `lib/ui/common/`, `lib/core/`, `lib/ui/core/`, `lib/ui/navigation/` → `docs/commons-candidates.md` (manifest lives in khelam). Slices: 1 = 9 atomic widgets (5 NEW, 3 dedup-identical to forkable, 1 divergent `feedback` — khelam's `!`-free version canonical); 2 = `dio_api_client` (divergent, child extended); 3 = theme (4 files); 4 = navigation (deferred).
- **v0.1.0 — widgets (Phases 3–5)**: 9 widgets + 2 test files (`bottom_sheet`, `phone_input`; 15 tests) extracted into `lib/src/widgets/`; barrel `lib/commons.dart`; template boilerplate removed; README documents the material name-collision convention. Commit `a8cb45a feat: initial commons package v0.1.0`, tag `v0.1.0` pushed. Analyze clean, **15/15** tests.
- **v0.2.0 — DioApiClient (Slice 2)**: `lib/src/network/dio_api_client.dart` (khelam's extended copy is canonical — forkable's old copy lacked `getJsonList`), `dio: ^5.9.2` dep, 6 tests moved → **21/21** tests. Commit `6378882 feat: add DioApiClient (v0.2.0)`, tag `v0.2.0` pushed.
- **v0.3.0 — theme infrastructure (Slice 3)**: `lib/src/theme/component_themes.dart` with palette-agnostic `ComponentThemes.build({colorScheme, textTheme, brightness, borderRadius, buttonShape})`; `lightColorScheme()`/`darkColorScheme()` REMOVED — projects own their `ColorScheme`. Monolith split into `lib/src/theme/groups/` (button, navigation, surface, input, selection, list, menu, picker, misc `_themes.dart`); `typography.dart` (`AppTypography`) moved as-is; barrel exports. Commit `415e414 feat: add shared theme builders (v0.3.0)`, tag `v0.3.0` pushed.

## Decisions
- **Clean single-commit import** (no history transfer) — extraction files are small; history stays in the child repo.
- **Palette-agnostic theme builders**: identity (AppPalette, ColorScheme seeds, `app_theme.dart`) stays per project; commons supplies only the shared component-building machinery.
- **Name collisions are intentional**: commons shadows material classes (`FilledButton`, `IconButton`, `OutlineButton`, `Title`); consumer convention = `material as m` or `hide` (documented in README).
- **Consumption pattern**: committed git dep `commons @ ref: vN` in each consumer; `pubspec_overrides.yaml` (gitignored) overrides locally for development — khelam uses an absolute path, forkable a relative `../commons`.

## Blockers
- None. (Reminders: `docs/commons.md` roster + `docs/commons-candidates.md` roadmap manifests live in khelam; this package keeps the version/tag history.)

## Next Steps
- **Navigation slice (Slice 4) — deferred, low priority**: `app_router.dart` is COUPLED (per-fork routes), `app_routes.dart` divergent; decided 2026-08-01 to stay per fork. Revisit only if a shared router skeleton is ever wanted.
- Future candidates from `docs/commons-candidates.md` as forks grow (e.g., new atomic widgets created in either project should be flagged for extraction).
